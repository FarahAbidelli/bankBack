package com.pfe.Bank.repository;

import com.pfe.Bank.model.ENUMERATION;
import org.springframework.data.jpa.repository.JpaRepository;

public interface VEnumRepository extends JpaRepository<ENUMERATION, Long> {
}
