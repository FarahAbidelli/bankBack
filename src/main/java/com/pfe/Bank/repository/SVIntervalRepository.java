package com.pfe.Bank.repository;

import com.pfe.Bank.model.INTERVALE;
import org.springframework.data.jpa.repository.JpaRepository;

public interface SVIntervalRepository extends JpaRepository<INTERVALE, Long> {
}
